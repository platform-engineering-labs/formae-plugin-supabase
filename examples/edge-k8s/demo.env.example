# Sourced manually (not auto-loaded). Fill in real values then:
#   set -a; . ./.demo.env; set +a
# This file is gitignored. Never commit a populated copy.

export SUPABASE_ACCESS_TOKEN=sbp_xxx
export SUPABASE_PROJECT_REF=your-project-ref
export SUPABASE_ORGANIZATION_ID=your-org-slug
export DEMO_IMAGE=edge-k8s-demo:latest
export INGRESS_HOST=edge-k8s-demo.local

# Optional — populated automatically by `make apply`. To run phase 2 by
# hand: export SUPABASE_ANON_KEY=$(make anon-key)
# export SUPABASE_ANON_KEY=
