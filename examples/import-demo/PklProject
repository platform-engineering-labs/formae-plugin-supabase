// PklProject for the Supabase import demo.
//
// Absolute paths so `formae extract --schema-location local` works:
// extract resolves the project from a tmp dir, so relative imports
// against `../../schema/...` would break.

amends "pkl:Project"

dependencies {
  ["supabase"] = import("/Users/stheno/.pel/formae/plugins/supabase/v0.1.0/schema/pkl/PklProject")

  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.84.0"
  }
}
